export default /*html*/ `
<div
    class="one-record-card"
    :class="{'editable': editable}"
>
    <div>
         <div
            class="picture-area"
        >
            <template
                v-if="improve.picture.length"
            >
                <div
                    v-if="improve.picture.length > 1"
                    class="prev-btn change-btn"
                    :class="{'active': currentPict > 0}"
                    @click="prevPictClick"
                ></div>
                <div
                    class="pict-content"
                >
                    <img
                        :src="imgSrc"
                    >
                </div>
                <div
                    v-if="improve.picture.length > 1"
                    class="next-btn change-btn"
                    :class="{'active': currentPict < (improve.picture.length - 1)}"
                    @click="nextPictClick"
                ></div>
                <div
                    class="count-text"
                >{{countText}}</div>
                <div
                    v-if="editable"
                    class="pict-options"
                    @click="pictMenuClick"
                >
                    <div
                        class="hover-notice-dark"
                        title-text="更多"
                    >
                        <div></div>
                    </div>
                    <div
                        class="pict-menu"
                        v-show="pictMenuShow"
                    >
                        <div
                            @click="addPictClick"
                        >新增照片</div>
                        <div
                            @click="selectPictClick"
                        >變更照片</div>
                        <div
                            @click="editPictClick"
                        >編輯照片</div>
                        <div
                            @click.stop="deletePictClick"
                        >刪除照片</div>
                    </div>
                </div>
            </template>
            <template
                v-else-if="editable"
            >
                <div
                    class="improve-add-pict-btn"
                    @click="addPictClick"
                >
                    <div>+</div>
                </div>
            </template>
            <template
                v-else
            >
                <div
                    class="pict-content"
                >
                    <img
                        style="filter: contrast(150%); height: 70%;"
                        src="./include/images/safety-inspect/image-icon.svg"
                    >
                </div>
            </template>
        </div>  
        <div
            class="form-area"
        >
            <div 
                form-type="endtime" 
                class="form-table"
            >
                <div>回覆日期</div>
                 <div
                    v-if="editable"
                >
                    <v-date-picker 
                        mode="date" 
                        v-model="improve.endTime"
                        :minDate="miniDate"
                        :masks="masks"             
                        :update-on-input="false"
                        color="yellow"
                    >
                        <template v-slot="{ inputValue, inputEvents }">
                            <input
                                class="date-input"
                                :value="inputValue"
                                v-on="inputEvents"
                            />
                        </template>
                    </v-date-picker>
                </div>
                <div
                    v-else
                >   
                    <input
                        class="date-input"
                        mode="text"
                        v-model="endTimeText"
                        :readonly="true"
                    >
                </div>
            </div>
            <div 
                form-type="improve-checker" 
                class="form-table"
            >
                <div>缺失改善人員</div>
                <div
                >
                    <div>{{checkerText}}</div>
                </div>
            </div>
            <div 
                form-type="improve-engineer" 
                class="form-table"
                :class="{'mouse-pointer': editable}"
                @click="selectClick('engineer')"
            >
                <div>責任工程師</div>
                <div>
                    <div>{{engineerText}}</div>
                </div>
            </div>
            <div 
                form-type="improve-company" 
                class="form-table"
                :class="{'mouse-pointer': editable}"
                @click="selectClick('company')"
            >
                <div>協力廠商</div>
                <div>{{companyText}}</div>
            </div>    
            <div 
                form-type="context" 
                class="form-textarea"
            >
                <div>改善說明</div>
                <div class="text">
                    <textarea
                    class="scroll-style-gray-thin"
                    v-model="improve.context" 
                    placeholder="請描述改善內容"
                    :readonly="!editable"
                    ></textarea>
                </div>
            </div>
            <div 
                form-type="context" 
                class="form-textarea"
            >
                <div>備註說明</div>
                <div class="text">
                    <textarea 
                    class="scroll-style-gray-thin"
                    v-model="improve.note"
                    placeholder="輸入內容"
                    :readonly="!editable"
                    ></textarea>
                </div>
            </div>
        </div>
        <input
            ref="fileInput"
            style="display:none;"
            type="file"
            accept="image/*"
            @change="uploadPict"
            multiple
        > 
        <input
            ref="changePictInput"
            style="display:none;"
            type="file"
            accept="image/*"
            @change="changePict"
        >
    </div>
    <div
        class="card-options"
        v-if="editable"
    >
        <div
            v-if="allowRemove"
            class="card-delete-btn hover-notice-dark"
            title-text="移除紀錄"
            @click="remove"
        >
            <div>+</div>
        </div>
    </div>
</div>
`